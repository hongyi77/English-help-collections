# 开发备忘（会话交接）

> 设计文档 `项目设计文档.md` 始终是最权威的功能说明，本文件只记**文档没写的进行中事项**。新对话开始时先读这两个文件。

## 当前状态（2026-08-31）

**Bug 修复:首页每日计数与「按日期」不一致**(用户真机报:8月31日首页「新学 20/20」,按日期只显示「当天新学 3 词」):
- **根因**:每日独立改造当天的旧档迁移把顶层**混库**的 `learnedToday`(各词库的词混在一起)整体塞进了切换前词库(cet4)的计数器——用户当天在多个词库测过学习,17 个他库词被计入四级;而「按日期」读的 history 条目带词库归属,是准的(真实四级新学=3)。
- **修法**:`app.js` 新增 `reconcileDailyFromHistory()`,启动时按当天 history(权威、带归属)重建所有库的 `learnedToday/reviewedToday`;写入侧本就计数+history 同步写(计数⊆history),重建不丢数;旧档顶层混库数组直接废弃。启动即对账 = 自愈一切历史污染。
- **修复后用户侧表现**:首页变回真实的「新学 3/20」(那 17 个在别的词库学的,各库各算各的),与按日期一致。
- 测试改写迁移断言+新增对账/自愈/重启保数断言,**286 项全过**;`sw.js`/`pwa-register.js` 升 **v20**,手机版已重打包。**v19(标识条)和 v20(本修复)都已提交,但按用户要求尚未推送**,等剩余问题一起发。

**自由拼写/听写新增「按日期」取词范围**（2026-08-31 晚，两轮定稿）：
- 范围 chips 新增「按日期」：取选定日期当天**新学**的词（当前词库，源 `history[日期].learned`，`app.js` 的 `wordsLearnedOn()` 过滤归属）；`SCOPES` 加 `date` 项，`wordsInScope(scope, kind)` 加 kind 参数（date 读 `spellDate`/`dictDate`，两模式各自记忆）。
- 日期选择 UI 第二轮定稿（用户要求去掉昨天/前天、按钮简洁、手机优先）：**紧凑单行**——日期按钮（中文「8月30日」+chevron，原生 date 输入透明覆盖其上，点按弹系统日历）+ 锚点小字（`当天新学 N 词 · 从 X 开始`，X=`wordsLearnedOn(日期)[0]` 即当天第一个新学的词；空则「该日无新学记录」）。CSS 在 index.html `.cfg-date-row/.cfg-date-btn/.cfg-date-input/.cfg-date-info`。
- 日期 UI 第三轮（用户要求能快速看哪天学过）：加**近期学习标识条** `.cfg-day-strip`——最近 14 天小格子（可横滑），学过词=墨蓝+圆点、选中=红圈、没学=浅灰，格子可点即选中那天（原生日历无法标注）。
- 空池提示按范围区分（`practiceEmptyHtml`：日期范围提示换一天）。选词器范围筛选同步支持（传 `pickerKind`）。
- 测试 284 项全过；`sw.js`/`pwa-register.js` 升 **v19**，手机版已重打包。
- 排查备注：调试时发现 `word` 一词不在四级词表（`LIB_WORD_SETS.cet4.has('word')===false`），写测试造数时别想当然用常见词，取 `LIBS.cet4.words[0][0]` 最稳。

**蝶变系列扩容 + 词库选择页两级分组**（用户定档）：
- 新词库「蝶变单词·初中版」（key=`diebian_junior`，源 `lib-sources/蝶变单词(初中版).txt`，2018 行/入库 1887）。教材按单元编排，同词多次出现带新词义（123 词）——**同词多义合并为一条释义**（「；」连接、相同释义去重，实际合并 128 处，如 match => `n.比赛；v.般配,相配`；用户先选过"保留首次"，最终改定**合并**）。原「蝶变单词」改名「蝶变单词（小学版）」，**key 仍是 `diebian` 不动，旧进度无损**。
- 词库选择页（设置页）改**两级手风琴**：`app.js` 新增 `LIB_GROUPS`（四六级=cet4/cet6、学段词汇=小初高、蝶变系列=两个蝶变），`ui.js` 的 `renderLibPicker` 重写为分组行+展开 chips（`toggleLibGroup`，默认展开当前词库所在组、单开互斥，箭头用 chevron-left 旋转）。分组只影响展示，词库互不合并。
- 测试 +6 项（分组行/单开/改名/初中版词数/释义合并/展开内容），**270 项全过**；`sw.js`/`pwa-register.js` 升 **v16**，手机版已重打包（1254KB）。

**同日早些时候：每日新学/复习目标改为按词库独立**（用户需求：四级学满不能阻断六级，各库各算各的）：
- state 结构变更：`learnedToday/reviewedToday` 从顶层移入 `state.libs[libKey]`；app.js 新增 `libDaily()/curDaily()` 辅助，`learnWord/reviewCorrect/reviewWrong/goalInfo/stats` 全部改走当前词库计数。
- 目标数值 `dailyNew/dailyReview` 仍是全局一份设置，对每个库各自生效（不做每库单独设目标）。
- 跨天重置：`today` 仍是全局一个标记，跨天时清空**所有**词库的每日计数。
- 旧档迁移：顶层全局计数在 `loadState()` 迁入切换前所在词库（`settings.lib`）。
- 学习记录(history)仍全局，条目带词库归属——未动。导入不合并每日计数（导入不占用当日额度）。
- 测试 +16 项（隔离/阻断按库/旧档迁移/跨天清零），**264 项全过**；`sw.js`/`pwa-register.js` 升 **v14**（必须同步部署），手机版已重打包。
- goalDoneHtml 完成页加了「各词库目标独立，切换词库可以继续学」提示；设置页词库说明文案同步。

**同日早些时候：新增第 6 个词库「蝶变单词」**（key=`diebian`，1191 词，`lib-sources/蝶变单词.txt` tab 分隔源）：
- `build-libs.js` 源解析改为 `readSource()`，同时支持 `{en,phone,cn}` JSON 和 tab 分隔 txt（txt 无音标属正常）。
- `vocab-libs.js` 重生成（670KB），手机版已重打包，当日 SW 先后升 v13/v14（随上面每日目标独立改动，**最终以 v14 一并部署**）。
- 与其他词库大量同词（四级 765/初中 1081/小学 600），按既有原则不做跨库去重，进度各存各的。
- 改动文件：build-libs.js、vocab-libs.js、sw.js、pwa-register.js、两份文档、手机版重打包。（已随 788b96d/1a40357 提交推送）

## 上一轮（2026-08-30 深夜）

**Edge 朗读已定档为唯一在线音源**（用户拍板：有道/百度"在线真人"音源整体移除），测试 248 项全过，手机版已重打包。发音引擎两层：Edge 朗读（男女音色自选）→ 设备TTS 兜底。本次新增：
- **IndexedDB 永久缓存**（`cet4-edge-tts` 库）：Edge 合成的 MP3 Blob 跨会话保存，合成过的词离线秒播；任何失败静默降级为纯内存缓存。`edgeSynthesize` 带在途去重（预取+立即播同文本只发一条连接）。
- **预取** `prefetchUpcomingAudio()`：呈现新词时（识别/拼写/听写/自由拼写四个渲染函数挂了钩子）自动把后面 4 个词提前合成好（串行队列 `edgePrefetchChain`）；"第一次听到"也不等网络。
- **自动轮播去掉第 3 轮强化朗读**，与判分/自查节奏一致（每词两轮 = 单词×4 + 汉译×2）。
- 有道/百度链路（`onlineVoiceUrl`/`audioPlayAwait`/口音设置 audioAcc/SW 音频拦截与 `cet4-audio-v1` 缓存）全部删除；旧存档 `voiceSrc:'youdao'` 自动迁移为 `'edge'`；`sw.js` 升 `v12`（与 pwa-register 同步）。
- 曾评估过"离线语音包"（批量预下载全部词库发音，约 100MB），用户最终放弃，未实现。

## 待办 / 未决

1. **git 提交 + 推送**（用户会再确认一次；改动=app.js/ui.js/index.html/sw.js/pwa-register.js/test-logic.js/两份文档/手机版重打包）。
2. **真机验证**（用户手机是 Edge）：① 学新词时后续词预取生效（第一次听到不等网络）② 自动轮播两轮节奏 ③ 设置页音源只剩「Edge朗读/设备TTS」两项、旧存档无感迁移 ④ 熄屏续播。
3. **仓库清理**：用户自己提交了 `四级词汇通.zip`（3.5MB，提交 5a4dc77）进仓库，建议移除（本地保留），用户未答复。

## Edge 朗读关键实测结论（2026-08-30，排查别绕弯）

- **Sec-MS-GEC token 必须用 float64 运算**：ticks≈1.3e17 超出 2^53，浮点舍入后的值才是服务端期望值；用 BigInt 精确计算得到「教科书正确」的值反而 403。此坑与 Python 官方库 `rany2/edge-tts` 的 drm.py 对拍确认。
- **服务端校验浏览器真实 UA**：须含 `Edg/`（桌面）/`EdgA/`（安卓）/`EdgiOS/`（iOS）且主版本 ≥135（实测 130 被拒、135/138/140/143 放行）。UA 是浏览器自动带的、网页改不了 ⇒ **该音源只在 Edge 浏览器里可用**，其他浏览器 `isEdgeBrowser()` 直接判不可用（UI 隐藏 Edge 选项，运行时自动回落设备TTS）。Origin/Cookie/ConnectionId 均不校验，token/版本走 URL 查询参数。
- CSP `connect-src` 放行 `wss://speech.platform.bing.com`，用页面内 `securitypolicyviolation` 事件实测验证过（对照域名被拦、目标域名放行）。
- 失败回落已真浏览器验证：服务端拒绝 → `edgePlayAwait` 返回 false → 无残留连接 → 链条落到设备TTS。
- 官方音色清单可用 `https://speech.platform.bing.com/consumer/speech/synthesize/readaloud/voices/list?trustedclienttoken=6A5AA1D4EAFF4E9FB37E23D68491D6F4`（带 Edge UA）拉取，`EDGE_VOICES` 的 16 个 ShortName 已全部对照校验过。
- **装讯飞语记等第三方 TTS 引擎对安卓 Chrome/Edge 的 speechSynthesis 无效**（浏览器不暴露第三方引擎声音）——手机上自选男女声只能走 Edge 音源这条路。

## 关键技术事实（排查时别绕弯）

- GitHub Pages 给 sw.js 的 HTTP 缓存 max-age=600，`pwa-register.js` 里 `sw.js?v=N` 的 N 必须和 `sw.js` 的 `CACHE_VER` 同步改，否则换版卡 10 分钟。**v11 起安装/同源刷新都带 `cache:'reload'`**：此前部署窗口期 HTTP 缓存会把旧 index.html 装进新 SW 缓存（旧页面+新 JS 混版，旧 onclick 调不到改名后的函数，症状=设置页点击无反应，2026-08-30 用户真机踩过）。
- 非 Edge 浏览器（微信/QQ/Chrome）里 Edge 音源不可用，设备又没系统 TTS 时就是无声——听写入口守卫 `canSpeakHere()` 会隐藏入口。
- Edge 音频缓存在页面侧 IndexedDB，SW 管不到；SW 只管静态资源。别再往 sw.js 里加音频拦截（在线真人音源已删）。
- 本地测试：`启动服务器.bat`（localhost:8080）；改完代码跑 `node test-logic.js`，改完任何代码要 `node build-single.js` 重打包手机版。
